#!/usr/bin/env bash

set -e

DOTFILES=${DOTFILES:-"$HOME/.dotfiles"}

# shellcheck source=../.utils/functions.sh
source "${DOTFILES}/.utils/functions.sh"

load_env_file

force_sudo

# install / update everything
"${DOTFILES}/bin/update"

# TODO check for git user/email
